#ifndef VERSION_H
#define VERSION_H

#pragma once

#include <string>

namespace warhawk
{
  struct Version
  {
    static inline const std::string GIT_HASH = "@GIT_HASH@";
    static inline const std::string GIT_DATE = "@GIT_DATE@";
  };
}

#endif // VERSION_H
